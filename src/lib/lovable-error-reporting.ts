// Stub — Lovable telemetry removed.
// Replace with your own error reporting (e.g. Sentry) when ready.
export function reportLovableError(_error: unknown, _context: Record<string, unknown> = {}) {
  // no-op for now
}
